name: Continuous Integration
on: [push]                      # Will make the workflow run every time you push to any branch

jobs:
  build:
    name: Build and test
    runs-on: ubuntu-latest      # Sets the build environment a machine with the latest Ubuntu installed
    steps:
    - uses: actions/checkout@v2 # Adds a step to checkout the repository code
    - name: Build .NET code
      run: dotnet build

    - name: Test .NET code
      run: dotnet test

    - name: Install NPM
      working-directory: ./DotnetTemplate.Web
      run: npm install

    - name: Build TypeScript code
      working-directory: ./DotnetTemplate.Web
      run: npm run build

    - name: Run TypeScript linting 
      working-directory: ./DotnetTemplate.Web
      run: npm run lint

    - name: Test TypeScript code
      working-directory: ./DotnetTemplate.Web
      run: npm t
    
    - name: Teams Notification
      uses: https://mclaren.webhook.office.com/webhookb2/d0317a5d-1d29-4024-8555-24c10f780afa@26583ed4-9ffc-46c2-9d1a-f0ca35a30a8d/IncomingWebhook/ad54858e070d473f8487b3cd2ae15d0d/6dea6374-5f7d-41e2-957f-4867dcd6c322
      with:
       Great Success


